services:
  podsync:
    image: mxpv/podsync:latest
    container_name: podsync
    volumes:
      - ./config:/app/config
      - ./data:/app/data
    ports:
      - "8080:8080"
    restart: unless-stopped

  # Local mode: sidecar on the same host with bind mounts
  podsync-ui:
    build: .
    container_name: podsync-ui
    volumes:
      - ./config:/app/config
      - ./data:/app/data:ro
      - /var/run/docker.sock:/var/run/docker.sock
      - sidecar-config:/app/sidecar-config
    ports:
      - "3000:3000"
    environment:
      - PODSYNC_MODE=local
      - PODSYNC_CONTAINER_NAME=podsync
      - PODSYNC_CONFIG_PATH=/app/config/config.toml
      - PODSYNC_DATA_DIR=/app/data
    depends_on:
      - podsync
    restart: unless-stopped

volumes:
  sidecar-config:
